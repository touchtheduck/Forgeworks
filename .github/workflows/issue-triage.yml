name: "Issue Triage"
on:
  issues:
    types: [opened]

permissions:
  issues: write
  contents: read

jobs:
  issue-triage:
    runs-on: ubuntu-latest
    steps:
      - uses: github/issue-labeler@v3.4
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          configuration-path: .github/issue-labeler.yml
          not-before: 2025-08-13T22:42:18.307Z
          enable-versioned-regex: 0
